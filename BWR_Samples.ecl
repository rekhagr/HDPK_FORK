IMPORT Visualizer;
IMPORT sample1;

OUTPUT(sample1.two.twoDs,NAMED('SampleOut'));
Visualizer.MultiD.Bar('SampleVisu', /*datasource*/, 'SampleOutNew', /*mappings*/, /*filteredBy*/, /*dermatologyProperties*/ );
